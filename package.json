{
  "name": "jyotish-service",
  "version": "0.1.0",
  "private": true,
  "description": "Premium web service wrapper for the deterministic Jyotish engine.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/chart3d.mjs",
    "test:web": "node tests/web-static.check.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
