# Direct runtime + dev dependencies — versions pinned to the uv-resolved set.
# uv.lock (once populated) holds the full transitive lock; requirements.txt lists
# only direct deps so humans can read and audit them.
pyswisseph==2.10.3.2
pydantic==2.13.4
python-dateutil==2.9.0.post0
pytz==2026.2
tzdata==2026.2
timezonefinder==8.2.0
geopy==2.4.1
typer==0.26.8
rich==15.0.0
pytest==9.1.1
jinja2==3.1.6
